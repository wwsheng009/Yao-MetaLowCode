function queryChartData({
  chartType,
  entityName,
  latitude,
  longitude,
  noPrivileges,
  filter,
}) {}

function updateDefault(id, defaultChart) {}

function getEntityCode(query) {
  
}
